/**
 * 
 * This package contains classes that handle the startup/shutdown and
 * initialization of the situation handler.
 * <p>
 * The situation handler can be started as java application or it can be run on
 * a server (therefore, there are two different paths for the startup).
 * 
 * @author Stefan
 *
 */
package main;